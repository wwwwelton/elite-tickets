import { SiteShell } from "@/components/shell/site-shell";

export default function GateHomePage() {
  return (
    <SiteShell
      title="Gate"
      subtitle="Select an event and validate tickets quickly."
    >
      <section />
    </SiteShell>
  );
}
