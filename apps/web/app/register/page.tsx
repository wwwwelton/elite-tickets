import { SiteShell } from "@/components/shell/site-shell";

export default function RegisterPage() {
  return (
    <SiteShell
      title="Create account"
      subtitle="Customer registration will reflect the verified backend contract."
    >
      <section />
    </SiteShell>
  );
}
